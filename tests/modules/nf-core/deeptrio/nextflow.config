process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: DEEPTRIO {
        ext.args = ' --regions=\"chr20\" --model_type=WGS '
        ext.prefix = { "${meta.id}_out" }
    }

}
